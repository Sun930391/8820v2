name: Build Custom Padavan for ZTE-E8820V2

on:
  workflow_dispatch:  # 手动触发

env:
  FIRMWARE_AUTHOR: YourName
  TARGET_DEVICE: ZTE-E8820V2
  TOOLCHAIN: mipsel-linux-musl  # 使用 musl 工具链，与您 xray 兼容性更好
  TOOLCHAIN_URL: https://github.com/TurBoTse/padavan/releases/download/toolchain/mipsel-linux-musl.tar.xz

jobs:
  build:
    name: Build Firmware
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@main
        with:
          fetch-depth: 1

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            bison flex gperf help2man libtool-bin texinfo python3-docutils autopoint gettext \
            autoconf automake liblzma-dev libltdl-dev liblzo2-dev jq ccache wget

      - name: Prepare Toolchain
        run: |
          wget -O toolchain.tar.xz ${TOOLCHAIN_URL}
          sudo mkdir -p /opt
          sudo tar -xJf toolchain.tar.xz -C /opt/
          echo "TOOLCHAIN_PATH=/opt/${{ env.TOOLCHAIN }}" >> $GITHUB_ENV

      - name: Customize Configuration (Enable USB, Modem, PPP & xray)
        run: |
          CONFIG_FILE="trunk/configs/templates/${{ env.TARGET_DEVICE }}.config"
          
          # 确保配置文件存在
          if [ ! -f "$CONFIG_FILE" ]; then
            echo "Error: Configuration file $CONFIG_FILE not found!"
            exit 1
          fi

          # 追加 USB、存储、调制解调器和 PPP 支持
          cat << EOF >> "$CONFIG_FILE"
          # USB Storage Support
          CONFIG_USB_SUPPORT=y
          CONFIG_USB_OHCI=y
          CONFIG_USB_EHCI=y
          CONFIG_USB_XHCI=y
          CONFIG_USB_STORAGE=y
          CONFIG_FIRMWARE_INCLUDE_NTFS_3G=y
          CONFIG_FIRMWARE_INCLUDE_EXFAT=y
          CONFIG_FIRMWARE_INCLUDE_SMBD36=y
          # USB Modem & PPP
          CONFIG_USB_SERIAL=y
          CONFIG_USB_SERIAL_OPTION=y
          CONFIG_USB_SERIAL_WWAN=y
          CONFIG_USB_SERIAL_QCARRIER=y
          CONFIG_PPP=y
          CONFIG_PPP_ASYNC=y
          CONFIG_PPP_SYNC_TTY=y
          CONFIG_PPP_DEFLATE=y
          CONFIG_PPP_MPPE=y
          CONFIG_FIRMWARE_INCLUDE_PPPD=y
          CONFIG_FIRMWARE_INCLUDE_CHAT=y
          EOF
          
          echo "Configuration appended to $CONFIG_FILE"

      - name: Download and Integrate xray
        run: |
          # 下载 xray
          XRAY_URL="https://github.com/Sun930391/padavan-4.4/raw/refs/heads/main/xray"
          wget -O /tmp/xray ${XRAY_URL}
          chmod +x /tmp/xray
          
          # 修改编译脚本，在打包前将 xray 复制到 romfs/usr/bin/
          cd trunk
          # 在 build_firmware_modify 中，于 make 命令后插入复制操作
          sed -i '/^[[:space:]]*make[[:space:]]/a \\tcp /tmp/xray $${ROMFSDIR}/usr/bin/\\n\\tchmod 755 $${ROMFSDIR}/usr/bin/xray' build_firmware_modify
          cd ..

      - name: Build Firmware
        run: |
          # 使用 Make 命令直接编译，并指定工具链路径
          export PATH=${TOOLCHAIN_PATH}/bin:${PATH}
          cd trunk
          # 清理并编译（假设 Makefile 支持以 TARGET 为参数）
          make ${{ env.TARGET_DEVICE }}
          cd ..

      - name: Prepare Output
        run: |
          mkdir -p trunk/output
          BUILD_DATE=$(TZ='Asia/Shanghai' date +%Y%m%d-%H%M)
          BASE_NAME="${FIRMWARE_AUTHOR}-${TARGET_DEVICE}-4.4-${TOOLCHAIN}-${BUILD_DATE}"
          i=1
          for f in trunk/images/*.trx; do
            if [ -f "$f" ]; then
              suffix=""
              [ $i -gt 1 ] && suffix="-$i"
              mv "$f" "trunk/output/${BASE_NAME}${suffix}.trx"
              i=$((i+1))
            fi
          done

      - name: Upload Firmware Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.TARGET_DEVICE }}-firmware
          path: trunk/output/*.trx
